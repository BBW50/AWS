import json

def myFunc01_handler(event, context): #関数の宣言
    x = int(event["x"])
    y = int(event["y"])
    print("x = " + str(x))
    print("y = " + str(y))
    retval = {"result" : x / y }    
    return json.dumps(retval)　#json形式で結果を返す
